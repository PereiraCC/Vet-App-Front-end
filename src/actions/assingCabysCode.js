import { types } from '../types/types';

export const OpenModalSearchProviders = () => ({ type: types.assingCabysCodeOpenModelSearchProviders });

export const CloseModalSearchProviders = () => ({ type: types.assingCabysCodeCloseModelSearchProviders });
